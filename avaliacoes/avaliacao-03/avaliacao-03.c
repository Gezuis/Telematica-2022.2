// Telematica-2022.2 (03/10/2022)
// Evoluindo na programação
#include <stdlib.h>
#include <stdio.h>
#include <windows.h>
#include <math.h>
 int main(){
 	system ("color A");
 	system ("title Funcoes Delta");
 	int a; //Delta maior que zero duas raízes diferentes.
 	int b; //Delta menor que zero não apresenta raiz.
 	int c; // Delta igual a zero duas raízes iguais.
 	a=1;
 	b=-2;
 	c=0;
	 if(a>0){
	 printf("\n Na funcao f(x) = x²+3x-4=0");
 	 printf("\n Delta e maior que zero duas raizes diferentes x¹ = 1; x² = -4");
	 
 }
    else(c=0);{
    printf("\n Na funcao f(x) = x²-6x-9=0");
 	 printf("\n Delta e igual a zero duas raizes iguais x¹ = 3; x² = 3");	
}
	if(b<0) {
    	printf("\n Raízes Imaginárias");
			printf("\n Raízes Imaginárias");
				printf("\n Raízes Imaginárias");
 	    printf("\n Delta e menor que zero nao existe raiz ");
    }
    
    }
